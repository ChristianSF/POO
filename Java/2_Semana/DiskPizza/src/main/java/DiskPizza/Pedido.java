package DiskPizza;

import Pizzaria.Cliente;

public class Pedido {

    public static void main(String args[]) {
        // Verifica se o cliente já existe, se nao existir, cria outro
        if(numTelefone != 10){
            Cliente cliente1 = new Cliente();
            cliente1.setNome("Christian Freitas");
            cliente1.setCpf("123456789");
            cliente1.setNumTelefone(40028922);
            cliente1.setEndereco("Av Paulista");
        }
        
        else {
            // Fazer o Pedido
        }
    }
}
